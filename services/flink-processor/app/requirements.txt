# Consumer runtime. Lightweight on purpose so `docker compose up --build` stays
# fast: the ML paradigms (spark_ml, kstreams_onnx) run inference on onnxruntime +
# tokenizers only — no torch/pyspark/transformers (those live in src/training,
# offline). The remaining stubs (solver, langchain, n8n, mcp) add their own deps
# when wired.
confluent-kafka==2.6.1
psycopg2-binary==2.9.10
pydantic-settings==2.4.0
python-dotenv==1.0.1
# ML inference (spark_ml RandomForest + kstreams_onnx MLP, behind shared MiniLM)
onnxruntime==1.19.2
tokenizers==0.20.3
numpy==1.26.4
